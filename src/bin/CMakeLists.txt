include_directories(${CMAKE_CURRENT_BINARY_DIR}/../utils)
if(Hiactor_FOUND)
        add_executable(rt_server rt_server.cc)
        target_link_libraries(rt_server neug_server)
        install_without_export_neug_target(rt_server)
endif()

if(Hiactor_FOUND)
        include_directories(../engines/http_server)
        add_executable(rt_bench rt_bench.cc)
        target_link_libraries(rt_bench neug_server)
        install_without_export_neug_target(rt_bench)
endif()

if(Hiactor_FOUND)
        add_executable(interactive_server interactive_server.cc)
        target_link_libraries(interactive_server neug_server ${GFLAGS_LIBRARIES})
        if (OPENTELEMETRY_CPP_FOUND)
                target_link_libraries(interactive_server otel)
        endif()
        install_without_export_neug_target(interactive_server)
endif()


include_directories(${Boost_INCLUDE_DIRS})
add_executable(bulk_loader bulk_loader.cc)
target_link_libraries(bulk_loader neug_rt_mutable_graph ${GLOG_LIBRARIES} ${Boost_LIBRARIES} ${GFLAGS_LIBRARIES})
# If gcc <9, we need to link with stdc++fs
if (CMAKE_CXX_COMPILER_ID MATCHES "GNU" AND CMAKE_CXX_COMPILER_VERSION VERSION_LESS 9)
        target_link_libraries(neug_utils PUBLIC stdc++fs)
endif()

if (BUILD_WITH_OSS)
        target_link_libraries(bulk_loader cpp-sdk)
endif()
install_without_export_neug_target(bulk_loader)