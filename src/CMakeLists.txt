add_subdirectory(utils)
if (BUILD_ENGINE)
  add_subdirectory(storages)
  add_subdirectory(engines)
  if (BUILD_EXECUTABLES)
      add_subdirectory(bin)
  endif()
  add_subdirectory(planner)
endif()

if (BUILD_COMPILER)
    add_subdirectory(compiler)
    if (BUILD_ENGINE)
      add_subdirectory(main)
    endif()
endif()

if (BUILD_ALL_IN_ONE)
        message(STATUS "Compiler libraries: ${COMPILER_LIBRARIES}")
        add_library(neug_libraries SHARED ${ALL_OBJECT_FILES})
        target_link_libraries(neug_libraries ${COMPILER_LIBRARIES} ${GFLAGS_LIBRARIES} ${GLOG_LIBRARIES} ${Boost_LIBRARIES} ${Protobuf_LIBRARIES} ${YAML_CPP_LIBRARIES} ${ARROW_LIB} neug_grape_lite)
        if (BUILD_HTTP_SERVER)
            target_link_libraries(neug_libraries neug_graph_db_service ${GFLAGS_LIBRARIES} ${GLOG_LIBRARIES} ${Protobuf_LIBRARIES}  ${Boost_LIBRARIES})
            if (HTTP_SERVER_TYPE STREQUAL "brpc")
                target_link_libraries(neug_libraries ${BRPC_LIB} ${LEVELDB_LIB} ${OPENSSL_LIBRARIES} ${PROTOC_LIB})
            elseif (HTTP_SERVER_TYPE STREQUAL "hiactor")
                target_link_libraries(neug_libraries Hiactor::hiactor)
            endif()
        endif()
        if (APPLE)
            target_link_libraries(neug_libraries ${MACOS_DYLIBS})
        endif()
        target_include_directories(neug_libraries PUBLIC ${CMAKE_CURRENT_BINARY_DIR}/compiler)
endif()
