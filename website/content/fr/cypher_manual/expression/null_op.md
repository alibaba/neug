# Opérateur Null

Neug supporte `IS NULL` et `IS NOT NULL` pour filtrer les valeurs NULL générées pendant le calcul.

Opérateur | Description
---------|------------
IS NULL | true si la valeur évaluée est null
IS NOT NULL | true si la valeur évaluée n'est pas null