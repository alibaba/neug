# Null Operator

Neug unterstützt `IS NULL` und `IS NOT NULL`, um NULL-Werte zu filtern, die während der Berechnung entstehen.

Operator | Beschreibung
---------|------------
IS NULL | true, wenn der ausgewertete Wert null ist
IS NOT NULL | true, wenn der ausgewertete Wert nicht null ist