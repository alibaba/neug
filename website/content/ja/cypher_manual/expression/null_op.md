# Null Operator

Neug は計算中に生成された NULL 値をフィルタリングするために `IS NULL` と `IS NOT NULL` をサポートしています。

Operator | Description
---------|------------
IS NULL | 評価された値が null の場合に true を返す
IS NOT NULL | 評価された値が null でない場合に true を返す