if (BUILD_PYTHON)
    if (TARGET nexg_main AND TARGET nexg_planner)
        add_subdirectory(python_bind)
    else()
        message(WARNING "nexg_main or nexg_planner not found, skipping python bindings")
    endif()
endif()