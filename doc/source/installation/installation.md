# Install Neug

Neug is an embedded graph database with high performance, and is easy to use.

## Command Line


## Python

Neug python package are availabel at `pypi`, use `pip` to install the python package on Linux and macos. 

````{note}
Currently only support on python >= 3.8. TODO(zhanglei): Make this correct.
````
