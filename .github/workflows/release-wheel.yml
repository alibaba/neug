name: Release Wheels

on:
  workflow_dispatch:
    inputs:
      python_versions:
        description: 'Python version (e.g. 3.9) or range (e.g. 3.8-3.14) to build'
        required: false
        default: ''
      target_platform:
        description: 'Platform to build'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - linux-x86_64
          - linux-arm64
          - macos-arm64
          - windows-x86_64
      publish_to_pypi:
        description: 'Publish the built wheels to PyPI'
        type: boolean
        required: false
        default: true
  release:
    types:
      - created
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/release-wheel.yml'
      - '.github/workflows/wheels-common.yml'
      - 'tools/python_bind/setup.py'
      - 'tools/python_bind/pyproject.toml'
      - 'tools/python_bind/Makefile'
      - 'tools/python_bind/README.md'

concurrency:
  group: ${{ github.repository }}-${{ github.event.number || github.head_ref || github.sha }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  format_check:
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch' || github.event_name == 'release'
    uses: ./.github/workflows/format-check.yml
    with:
      is_pull_request: ${{ github.event_name == 'pull_request' }}

  build_wheels:
    needs:
      - format_check
    uses: ./.github/workflows/wheels-common.yml
    with:
      python_versions: ${{ inputs.python_versions }}
      target_platform: ${{ inputs.target_platform || 'all' }}
      mode: release
      set_nightly_version: false
      pybind11_findpython_off: true
      linux_x86_python: '3.13'
      linux_arm_python: '3.13'
      mac_python: '3.13'
      windows_python: '3.11'
      cibuildwheel_version: '3.4.0'
      enable_macos_x86: false
      caller_event_name: ${{ github.event_name }}
      # Undefined on release/pull_request triggers; '|| false' keeps the value boolean.
      publish_to_pypi: ${{ inputs.publish_to_pypi || false }}
    secrets: inherit
