---
description: Update spec with new requirements.
---
## User Input

```text
$ARGUMENTS
```

You **MUST** consider the user input before proceeding (if not empty).

## Understanding User Input

1. If user want to update some details in a module, just change the content.
2. If user want to add or remove a module, the ID of the afterward module and the overview figure should also be updated.
3. Wait for user's review of updated content.

## Update Tasks.md

1. Any update in `spec.md` must has related update in `tasks.md`.
2. If the module number is changed, `tasks.md` need to be synchronized.