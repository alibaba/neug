if (BUILD_HTTP_SERVER)
        add_executable(rt_server rt_server.cc)
        message(STATUS "openssl library: ${OPENSSL_LIBRARIES}")
        if (APPLE)
                target_link_libraries(rt_server PRIVATE ${MACOS_DYLIBS})
        endif() 
        target_link_libraries(rt_server PRIVATE ${Protobuf_LIBRARIES} ${BRPC_LIB} ${LEVELDB_LIB} ${OPENSSL_LIBRARIES} ${GFLAGS_LIBRARIES} ${GLOG_LIBRARIES} ${CMAKE_THREAD_LIBS_INIT})
        target_link_libraries(rt_server PRIVATE neug_libraries)
        install_without_export_neug_target(rt_server)
endif() 


add_executable(benchmark benchmark.cc)
target_link_libraries(benchmark PUBLIC neug_libraries)
target_link_libraries(benchmark PUBLIC ${ARROW_LIB} ${Boost_LIBRARIES} ${GLOG_LIBRARIES} ${GFLAGS_LIBRARIES} ${Protobuf_LIBRARIES} ${YAML_CPP_LIBRARIES})
install_without_export_neug_target(benchmark)
